[main]
no-reply-email = no-reply@meon.eu

[domains]
localhost = localhost, www.some-alias-domain.name-example
bootstrap = bootstrap
search_t = mw-search-test.local

[opensearch]
nodes = http://localhost:9200
# cxn_pool = Async::Sniff
# basic_auth = user:secret
